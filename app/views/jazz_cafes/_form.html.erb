<%= form_with(model: jazz_cafe) do |form| %>
  <% if jazz_cafe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(jazz_cafe.errors.count, "error") %> prohibited this jazz_cafe from being saved:</h2>

      <ul>
        <% jazz_cafe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
