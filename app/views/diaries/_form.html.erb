<%= form_with(model: diary, data: { controller: "auto-complete"}) do |form| %>
  <% if diary.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(diary.errors.count, "error") %> prohibited this diary from being saved:</h2>

      <ul>
        <% diary.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :jazz_cafe_name, style: "display: block" %>
    <%= form.text_field :jazz_cafe_name, value: @jazz_cafe_name, id: "jazzCafeComplete" %>
  </div>

  <div>
    <%= form.label :record_title, style: "display: block" %>
    <%= form.text_field :record_title, value: @record_title, id: "recordComplete" %>
  </div>

  <div>
    <%= form.label :artist_name, style: "display: block" %>
    <%= form.text_field :artist_name, value: @artist_name, id: "artistComplete" %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
